/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  images: {
    domains: ["randomuser.me"],
  },
  images: {
    unoptimized: true,
  },
};
export default nextConfig;
